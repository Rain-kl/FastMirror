# FastMirror 配置文件示例
# 复制此文件为 .env 并修改配置

# 运行模式: proxy (反代模式) 或 local (本地模式)
MODE=proxy

# 监听配置
HOST=0.0.0.0
PORT=8000

# 目标服务器 URL (反代模式必需)
TARGET_URL=http://example.com

# 缓存目录配置
CACHE_DIR=./cache
CACHE_POST_DIR=./cache_post

# 定制化接口目录
CUSTOM_ROUTES_DIR=./custom_routes

# 请求超时时间(秒)
REQUEST_TIMEOUT=30

# 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
